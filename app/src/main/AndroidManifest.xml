<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.davidferrand.spark">

    <application
        android:name=".app.SparkApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.setupmeter.SetupMeterActivity"
            android:label="@string/setup_meter_activity_label" />
        <activity
            android:name=".ui.addreading.AddReadingActivity"
            android:label="@string/add_reading_activity_label" />
    </application>

</manifest>